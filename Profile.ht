<!DOCTYPE html>
<html lang="pt">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Perfil do Cliente - BarbeariaGn</title>
  <style>
    body {
      background:#0f172a;
      color:white;
      font-family:Arial;
      padding:20px;
    }
    h1 { color:#facc15; }
    .box {
      background:#1e293b;
      padding:20px;
      border-radius:10px;
      margin-bottom:20px;
      font-size:18px;
    }
    label {
      font-size:18px;
      display:block;
      margin-top:15px;
    }
    input {
      width:100%;
      padding:12px;
      border-radius:8px;
      border:none;
      margin-top:5px;
    }
    button {
      background:#facc15;
      padding:15px;
      border:none;
      border-radius:8px;
      width:100%;
      font-size:18px;
      margin-top:20px;
    }
    a { color:#facc15; text-decoration:none; }
  </style>
</head>
<body>

  <h1>Perfil do Cliente</h1>

  <div class="box">
    <label>Nome:</label>
    <input type="text" id="nome" placeholder="Digite seu nome">

    <label>Telefone:</label>
    <input type="text" id="telefone" placeholder="(DDD) 00000-0000">

    <label>Data de Nascimento:</label>
    <input type="date" id="nascimento">

    <button onclick="salvar()">Salvar</button>
  </div>

  <p><a href="index.html">← Voltar</a></p>

  <script>
    function salvar() {
      alert('Informações salvas! (Funcionalidade real será conectada ao backend)');
    }
  </script>

</body>
</html>
